# Supabase (client-safe)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase (server-only secrets — never expose to the client bundle)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=

# BSN encryption key, stored as a Supabase Vault secret in production.
# For local development only — see docs/deployment.md before going to production.
BSN_ENCRYPTION_KEY=

# Resend (Fase 10)
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# AFAS (Fase 11 — not used yet, placeholder for future integration)
AFAS_ENVIRONMENT_ID=
AFAS_TOKEN=
